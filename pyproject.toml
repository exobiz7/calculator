[project]
name = "calculator"
version = "0.1.0"
description = "Multi-mode practical calculator (basic / scientific / financial)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
