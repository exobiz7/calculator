"""Exponent (power) calculator module."""


def square(x: float) -> float:
    """Return x squared (x ** 2)."""
    return x**2


def power(x: float, n: float) -> float:
    """Return x raised to the n-th power (x ** n)."""
    return x**n
